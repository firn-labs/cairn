services:
  cairn:
    build: .
    ports:
      - '3000:3000'
    env_file: .env
    environment:
      DATABASE_PATH: /app/data/cairn.db
    volumes:
      - cairn-data:/app/data
    restart: unless-stopped

volumes:
  cairn-data:
